name: code_generator
description: 代码生成助手 - 对接后端的现代风格前端
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0  # 网络请求
  flutter_highlight: ^0.7.0  # 代码高亮
  highlight: ^0.7.0          # 语法高亮核心库
  file_picker: ^6.1.1
  provider: ^6.1.1          # 添加 provider 依赖

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true